name: Build and Push Docker Image

on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

  # Allows you to reuse workflows by referencing their YAML files
  workflow_call:

jobs:
  build-and-push-docker:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write

    steps:

      - name: Log in to the Container registry
        uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@9ec57ed1fcdbf14dcef7dfbe97b2010124a938b7
        with:
          images: |
            ghcr.io/${{ github.repository }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: universite-gustave-eiffel/noisecapture:dev

#      - name: Run custom Docker build script
#        run: ./build_docker.sh
#        shell: bash
#        working-directory: ./onomap/deploy/docker
#
#      - name: Tag Docker Image
#        run: |
#          REPO_NAME=$(basename ${{ github.repository }})
#          IMAGE_NAME=ghcr.io/${{ github.repository_owner }}/$REPO_NAME
#          BRANCH_NAME=${{ github.head_ref || github.ref_name }}
#          TAG_NAME=$(echo $BRANCH_NAME | sed 's/[^a-zA-Z0-9._-]/-/g')
#          docker tag universite-gustave-eiffel/noisecapture:dev $IMAGE_NAME:$TAG_NAME
#          echo "image=$IMAGE_NAME:$TAG_NAME" >> $GITHUB_ENV


